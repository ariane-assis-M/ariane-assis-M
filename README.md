<?php
  $numero = readline("Digite um número: ");

  // Verifica se o número é divisível por 10
  if ($numero % 10 == 0) {
    echo "O número $numero é divisível por 10.";
  }

  // Verifica se o número é divisível por 5
  else if ($numero % 5 == 0) {
    echo "O número $numero é divisível por 5.";
  }

  // Verifica se o número é divisível por 2
  else if ($numero % 2 == 0) {
    echo "O número $numero é divisível por 2.";
  }

  // Informa que o número não é divisível por nenhum dos números
  else {
    echo "O número $numero não é divisível por 10, 5 ou 2.";
   
  }
  
   $numero = readline("Digite um número: ");
   
    // Verifica se o número é divisível por 10
  if ($numero % 10 == 0) {
    echo "O número $numero é divisível por 10.";
  }

  // Verifica se o número é divisível por 5
  else if ($numero % 5 == 0) {
    echo "O número $numero é divisível por 5.";
  }

  // Verifica se o número é divisível por 2
  else if ($numero % 2 == 0) {
    echo "O número $numero é divisível por 2.";
  }

  // Informa que o número não é divisível por nenhum dos números
  else {
    echo "O número $numero não é divisível por 10, 5 ou 2.";
   
  }
  
  $numero = readline("Digite um número: ");

  // Verifica se o número é divisível por 10
  if ($numero % 10 == 0) {
    echo "O número $numero é divisível por 10.";
  }

  // Verifica se o número é divisível por 5
  else if ($numero % 5 == 0) {
    echo "O número $numero é divisível por 5.";
  }

  // Verifica se o número é divisível por 2
  else if ($numero % 2 == 0) {
    echo "O número $numero é divisível por 2.";
  }

  // Informa que o número não é divisível por nenhum dos números
  else {
    echo "O número $numero não é divisível por 10, 5 ou 2.";
   
  } 
?>
